import React from "react";

import Step from "./Step";

const Step1 = ({style}) => (
	<Step styles={style} color="blue" order={1} title="Step 1">
		<p>This Step 1</p>
	</Step>
);

export default Step1;
